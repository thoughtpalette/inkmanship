/* Site controller */

angular.module( "ink.controllers" )

.controller( "Site", [ "$scope", "$rootScope", "$location",

    function ( $scope, $rootScope, $location )
    {
        "use strict";

        // w00t

    }

] );
